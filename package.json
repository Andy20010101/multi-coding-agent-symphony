{
  "name": "multi-coding-agent-symphony",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "description": "A BDD and TDD built orchestrator for multiple coding CLIs.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js src/adapters/*.js tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
