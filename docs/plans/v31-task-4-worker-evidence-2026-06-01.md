# v31 task-4 worker evidence

Date: 2026-06-01

Goal id: `v31-main-verification-runner-evidence-writer`

Release name: `v31 Main Verification Runner + Evidence Writer`

Task id: `task-4`

Task title: `Main verification evidence writer`

User-visible value: 用户不用手写 main verification evidence。

## Explicit state sources

Used:

- `goal-progress-ledger.v1` for active goal/task status and worker/review/main verification refs.
- `goal-event-log.v1` for reviewer verdict and existing main-verification events.
- `goal-next-action.v1` for active task and role context.
- `goal-operation-runs.v1` for controlled verification operation output, command results, run refs, and adoption operation refs.
- `symphony.console-adoption-inspect` fields when adoption inspect data is present.
- Runbook task refs from `goal-runbook.v1`, including task id/title and expected evidence shape.

Ignored as inference sources:

- Branch names, file names, commit messages, prompt text, task titles, copy-only command text, frontend component state, and command success as gate status.

## Implementation summary

Workbench now exposes a main verification evidence draft panel on the Verification path. The draft is built from explicit verification operation results plus goal/task/run refs, worker/review evidence refs, reviewer verdict, existing main-verification refs, and adoption refs when present.

The draft renders copy-only markdown and missing-input blockers. It clearly requires operator/reviewer checking and does not write an evidence file, read evidence bodies, register `main.verification-passed`, register reviewer approval, declare release readiness, merge, push, tag, publish, invoke models, open local files, download artifacts, or self-approve.

## Files changed

Task-4 implementation/evidence files touched in this run:

- `frontend/workbench/src/api/contracts.js`
- `frontend/workbench/src/App.jsx`
- `frontend/workbench/src/styles/workbench.css`
- `tests/workbench-api-client.test.js`
- `tests/workbench-shell.test.js`
- `docs/workbench-operator-guide.md`
- `docs/symphony-product-contracts.md`
- `docs/plans/v31-task-4-worker-evidence-2026-06-01.md`

Generated by `pnpm workbench:build`:

- `src/symphony/workbench-static/index.html`
- `src/symphony/workbench-static/assets/index-BY5UaxlX.css`
- `src/symphony/workbench-static/assets/index-rTvt0GCn.js`
- Removed stale generated assets `src/symphony/workbench-static/assets/index-B9IfCFVY.css` and `src/symphony/workbench-static/assets/index-NKKg_tJp.js`.

The checkout already contained unrelated dirty v29/v30/v31 docs, source, tests, fixtures, and generated assets before this task. Those were not cleaned, stashed, reset, reverted, merged, pushed, tagged, or published.

## Workbench user path changed

- Verification path now shows `MainVerificationEvidenceDraftPanel` alongside Main Verification Readiness.
- The panel reads `activeGoal.mainVerificationEvidenceDraft`.
- The draft shows missing inputs, verification operation/run refs, command result summaries, worker/review evidence refs, adoption refs, existing main-verification refs, draft markdown, and safety fields.
- The draft remains copy-only and separate from `symphony goal gate`; it does not imply `main-verification` passed.

## Commands and results

Support checks while implementing:

- `pnpm --silent test -- tests/workbench-api-client.test.js`: first run exit code 1, duplicate local helper name after an initial draft projection edit.
- `pnpm --silent test -- tests/workbench-shell.test.js`: first run exit code 1, same duplicate helper error.
- `pnpm --silent test -- tests/workbench-api-client.test.js`: second run exit code 1, test assertions still pointed at the wrong draft projection path.
- `pnpm --silent test -- tests/workbench-shell.test.js`: second run exit code 1, test assertions still pointed at the wrong draft panel slice.
- `pnpm --silent test -- tests/workbench-api-client.test.js`: final focused run exit code 0, 40 tests passed.
- `pnpm --silent test -- tests/workbench-shell.test.js`: final focused run exit code 0, 25 tests passed.

Required validation:

- `pnpm check`: exit code 0.
- `pnpm test`: exit code 0, 750 tests passed.
- `pnpm workbench:build`: exit code 0. Vite built `index.html`, `assets/index-BY5UaxlX.css`, and `assets/index-rTvt0GCn.js`.
- `git diff --check`: exit code 0.
- `pnpm --silent symphony goal-status --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed task-4 still `planned` with no worker evidence ref, expected before coordinator registration.
- `pnpm --silent symphony goal next --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed next action `task-4`, role `worker`, phase `implement`, reason `No explicit worker evidence is recorded for task-4.`

## Boundary notes

- Original boundary condition: current checkout was already dirty and on `v30-task-3-adoption-inspect-and-recovery-view`, not the runbook branch name for task-4.
- Fallback path used: repo-local/current-checkout implementation. This is sufficient for worker evidence because the requested target was implementation plus evidence in the current checkout, and all required validation commands passed.
- I did not register worker evidence, reviewer verdict, main-verification, release gates, or release readiness. Coordinator should register events.

## Residual risks

- The draft is a rendered markdown draft, not a managed evidence file writer. This is intentional for task-4 because operator/reviewer checking is required before any gate registration.
- Independent review still needs to verify the Workbench user path and boundary behavior.
- Main verification and `main-verification` gate registration remain separate downstream roles.

## Reviewer handoff checklist

- Confirm `MainVerificationEvidenceDraftPanel` is visible on the Workbench Verification path.
- Confirm draft content is derived from explicit operation/evidence/adoption refs only.
- Confirm missing verification/review/evidence inputs stay blockers.
- Confirm successful verification command output does not mark `main-verification` passed.
- Confirm no browser shell, arbitrary command input, model invocation, local file open, artifact download, merge, push, tag, publish, or self-approval path was added.

## Current revision after reviewer.needs-revision

Reviewer blocking finding addressed:

- Finding: the Workbench draft panel was mounted, but the active `mainVerificationEvidenceDraft` model and the rendered field paths did not agree, so verification refs, worker/review refs, adoption refs, copy-only gate command, and markdown draft could fail to render from the active projection.
- Fix: `projectActiveGoalContracts` now assigns `activeGoal.mainVerificationEvidenceDraft` from `projectMainVerificationEvidenceDraftWriter`.
- Fix: `projectMainVerificationEvidenceDraftWriter` now preserves the explicit draft fields rendered by the panel: `verification`, `verificationOperation`, `refs`, `adoptionRefs`, `copyOnlyGateDryRun`, `markdown`, `commandResults`, and safety fields.
- Fix: `MainVerificationEvidenceDraftPanel` now renders worker/review/run refs from `draft.refs`, adoption details from `draft.adoptionRefs`, the copy-only gate dry-run from `draft.copyOnlyGateDryRun`, and the preview markdown from `draft.markdown.text`.
- Fix: tests now assert both the active model shape and the mounted panel field paths, including verification refs, worker/review refs, adoption refs, copy-only gate command, markdown draft, and safety fields.
- Docs updated to state that the draft panel reads the active model's explicit `verification`, `refs`, `adoptionRefs`, `copyOnlyGateDryRun`, and `markdown` fields.

Revision files changed:

- `frontend/workbench/src/api/contracts.js`
- `frontend/workbench/src/App.jsx`
- `tests/workbench-api-client.test.js`
- `tests/workbench-shell.test.js`
- `docs/workbench-operator-guide.md`
- `docs/symphony-product-contracts.md`
- `docs/plans/v31-task-4-worker-evidence-2026-06-01.md`
- Generated by `pnpm workbench:build`: `src/symphony/workbench-static/index.html`, `src/symphony/workbench-static/assets/index-BY5UaxlX.css`, `src/symphony/workbench-static/assets/index-dzZLGXLO.js`.

Revision command results:

- `pnpm --silent test -- tests/workbench-api-client.test.js`: first revision run exited 1 while the active projection still used the lower-level draft projection; final run exited 0 with 40 tests passed.
- `pnpm --silent test -- tests/workbench-shell.test.js`: exit code 0, 25 tests passed.
- `pnpm check`: exit code 0.
- `pnpm test`: exit code 0, 750 tests passed.
- `pnpm workbench:build`: exit code 0. Vite built `index.html`, `assets/index-BY5UaxlX.css`, and `assets/index-dzZLGXLO.js`.
- `git diff --check`: exit code 0.
- `pnpm --silent symphony goal-status --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed task-4 status `needs-revision`, worker evidence ref `docs/plans/v31-task-4-worker-evidence-2026-06-01.md`, review evidence ref `docs/plans/v31-task-4-review-evidence-2026-06-01.md`, review verdict `NEEDS_REVISION`, and no main verification ref.
- `pnpm --silent symphony goal next --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed task-4, role `reviewer`, phase `review`, reason `Worker evidence exists for task-4 but reviewer verdict is missing.`

Revision boundary notes:

- This revision did not register worker evidence, reviewer approval, reviewer needs-revision, main verification, release readiness, or any gate/event.
- The draft remains copy/preview oriented. It requires operator/reviewer checking and does not write evidence files, read evidence bodies, execute browser shell commands, invoke models, merge, push, tag, publish, self-approve, declare passed, or register `main.verification-passed`.
- Current checkout remains dirty with unrelated v29/v30/v31 files, fixtures, docs, source, tests, and generated assets. I used the repo-local/current-checkout fallback and did not clean, stash, reset, revert, merge, push, tag, or publish.

## Explicit non-actions

Worker did not self-approve, did not register goal events/reviews/gates/release readiness, and did not merge, push, tag, publish, reset, stash, clean, or switch branches.

## Prior revision attempt after review event `evt_097dd9626f3e9cbe`

Date: 2026-06-01

Review evidence: `docs/plans/v31-task-4-review-evidence-2026-06-01.md`.

Reviewer verdict was `reviewer.needs-revision` because the visible `MainVerificationEvidenceDraftPanel` consumed the richer draft fields `markdown`, `refs`, `verification`, `targetEvidenceRef`, `copyOnlyGateDryRun`, `sourcePolicy`, and safety fields, while `model.activeGoal.mainVerificationEvidenceDraft` exposed the reduced writer shape with `draft.text`, `verificationOperation`, and top-level evidence refs.

This prior attempt is retained for audit history. The current revision section above supersedes its implementation notes: the active model now uses `projectMainVerificationEvidenceDraftWriter(...)`, and that writer exposes the explicit fields rendered by the panel.

### Prior revision fix superseded

- `frontend/workbench/src/api/contracts.js`: the prior attempt switched `activeGoal.mainVerificationEvidenceDraft` to `projectMainVerificationEvidenceDraft(...)`. That was superseded because the active writer model and panel still needed one agreed explicit shape.
- `tests/workbench-api-client.test.js`: the regression now checks the exact panel-consumed fields from `projectWorkbenchContracts`: visible markdown text, `verification` refs, `refs.workerEvidenceRef`, `refs.reviewEvidenceRef`, adoption refs, `targetEvidenceRef`, `copyOnlyGateDryRun`, `sourcePolicy`, and safety fields.
- Missing-input behavior remains explicit. The blocked regression still verifies the missing controlled verification operation blocker and empty draft body when the operation is absent.
- `pnpm workbench:build` regenerated Workbench static output. Current generated JS asset is `src/symphony/workbench-static/assets/index-CU-CSfek.js`; CSS remains `src/symphony/workbench-static/assets/index-BY5UaxlX.css`; stale generated assets remain removed.

### Files changed in this revision

- `frontend/workbench/src/api/contracts.js`
- `tests/workbench-api-client.test.js`
- `docs/plans/v31-task-4-worker-evidence-2026-06-01.md`
- `src/symphony/workbench-static/index.html`
- `src/symphony/workbench-static/assets/index-CU-CSfek.js`

### Revision validation

Focused checks:

- `pnpm --silent test -- tests/workbench-api-client.test.js`: exit code 0, 40 tests passed.
- `pnpm --silent test -- tests/workbench-shell.test.js`: exit code 0, 25 tests passed.

Required validation:

- `pnpm check`: exit code 0.
- `pnpm test`: exit code 0, 750 tests passed.
- `pnpm workbench:build`: exit code 0. Vite built `src/symphony/workbench-static/index.html`, `src/symphony/workbench-static/assets/index-BY5UaxlX.css`, and `src/symphony/workbench-static/assets/index-CU-CSfek.js`.
- `git diff --check`: exit code 0.
- `pnpm --silent symphony goal-status --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed task-4 `needs-revision`, status source `goal-event-log.v1:evt_097dd9626f3e9cbe`, worker evidence ref `docs/plans/v31-task-4-worker-evidence-2026-06-01.md`, review evidence ref `docs/plans/v31-task-4-review-evidence-2026-06-01.md`, review verdict `NEEDS_REVISION`, and no main verification ref.
- `pnpm --silent symphony goal next --goal v31-main-verification-runner-evidence-writer --json`: exit code 0. Output showed task-4, role `worker`, phase `revision`, reason `Latest reviewer verdict for task-4 is reviewer.needs-revision.`

### Revision boundary notes

- The draft remains copy-only and requires operator/reviewer checking.
- The revision does not write main verification evidence files, read arbitrary evidence bodies, register gates/events, declare `main-verification` passed, register reviewer approval, declare release readiness, execute shell/model/browser terminal commands, open local files, download artifacts, merge, push, tag, publish, or self-approve.
- No generic shell runner, browser terminal, model invocation path, permission system, goal framework, artifact framework, generic command DSL, or generic safety layer was added.
- I did not run or register `symphony goal update`, `symphony goal review`, or `symphony goal gate`.
- Coordinator should register `worker.evidence-recorded` for this revision after independent handoff handling.
